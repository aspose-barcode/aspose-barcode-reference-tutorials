---
date: 2026-02-04
description: Узнайте, как создавать изображения штрихкода Code128 на Java с помощью
  Aspose.BarCode, пример генерации штрихкода на Java, демонстрирующий сохранение в
  форматы JPEG, PNG, GIF и другие.
linktitle: Saving Barcode Images to Different Formats
second_title: Aspose.BarCode Java API
title: Как создать изображения штрихкода Code128 в Java с помощью Aspose.BarCode
url: /ru/java/advanced-settings-and-optimization/saving-barcode-images-different-formats/
weight: 13
---

{{< blocks/products/pf/main-wrap-class >}}
{{< blocks/products/pf/main-container >}}
{{< blocks/products/pf/tutorial-page-section >}}

# Как создавать изображения штрих‑кода code128 в Java с Aspose.BarCode

## Введение

Если вы ищете **как быстро и надёжно генерировать изображения штрих‑кода** в Java‑приложении, Aspose.BarCode for Java делает это без усилий. В этом руководстве мы пройдём через **пример генерации штрих‑кода на Java**, который **создаёт штрих‑код Code‑128** и сохраняет его в несколько популярных форматов изображений — JPEG, PNG, GIF и TIFF. К концу руководства вы точно будете знать, как **создавать файлы штрих‑кода code128**, конвертировать их в GIF, PNG или любой другой поддерживаемый формат и интегрировать процесс в более крупные Java‑проекты.

## Быстрые ответы
- **Какая библиотека нужна?** Aspose.BarCode for Java  
- **Поддерживаемые форматы вывода?** JPEG, PNG, GIF, TIFF, BMP и другие  
- **Типичное время реализации?** 5‑10 минут для базового примера  
- **Требования?** JDK 8+ и JAR‑файл Aspose.BarCode в classpath  
- **Можно ли изменить тип штрих‑кода?** Да — любой тип символьности, поддерживаемый Aspose.BarCode  

## Как создать штрих‑код code128 в Java

Создание **штрих‑кода code128** так же просто, как три строки кода после подключения библиотеки. Ниже мы разбираем каждый шаг, объясняем, почему он важен, и показываем, где вставить свои данные.

## Что такое генерация штрих‑кода в Java?

Генерация штрих‑кода — это процесс преобразования буквенно‑цифровых данных в визуальный шаблон, читаемый машиной. Разработчикам Java часто требуется эта возможность для систем учёта, билетов или обработки платежей. Aspose.BarCode абстрагирует детали низкоуровневого кодирования, позволяя сосредоточиться на бизнес‑логике.

## Почему стоит использовать Aspose.BarCode for Java?

- **API без зависимостей** — без нативных библиотек и внешних сервисов.  
- **Высококачественная отрисовка** — чёткие изображения даже при большом DPI.  
- **Широкая поддержка форматов** — от растровых (PNG, JPEG) до векторных (SVG, PDF).  
- **Готово для предприятий** — оптимизировано для массовой генерации и многопоточных сред.  

## Требования

Прежде чем начать, убедитесь, что у вас есть:

- **Java Development Kit (JDK) 8 или новее** установлен и настроен `JAVA_HOME`.  
- **Библиотека Aspose.BarCode for Java**, скачанная со [страницы официальных релизов](https://releases.aspose.com/barcode/java/).  
- **IDE для Java**, например IntelliJ IDEA, Eclipse или VS Code (необязательно, но рекомендуется).  

## Пошаговое руководство

### Шаг 1: Импортировать необходимые пространства имён

Сначала добавьте необходимые импорты в ваш Java‑класс. Они подключают основные классы Aspose.BarCode и стандартные утилиты ввода‑вывода.

```java
import java.io.IOException;

import com.aspose.barcode.*;

import com.aspose.barcode.generation.BarcodeGenerator;
```

> **Совет:** Держите импорты упорядоченными; так код будет легче читать и избегать случайных конфликтов имён.

### Шаг 2: Установить путь к каталогу ресурсов

Определите папку, куда будут сохраняться сгенерированные изображения. Замените заполнитель на абсолютный или относительный путь, существующий на вашем компьютере.

```java
// The path to the resource directory.
String dataDir = "Your Document Directory";
```

> **Почему это важно:** Централизованное указание места вывода упрощает очистку и позволяет переиспользовать один и тот же путь в разных примерах.

### Шаг 3: Создать генератор штрих‑кода

Создайте объект `BarcodeGenerator`, укажите символьность (здесь `CODE_128`) и передайте данные, которые нужно закодировать.

```java
// Instantiate barcode object, set the symbology type to Code128 and set the code text.
BarcodeGenerator bb = new BarcodeGenerator(com.aspose.barcode.EncodeTypes.CODE_128, "1234567");
```

Вы можете заменить `EncodeTypes.CODE_128` на любой другой поддерживаемый тип (например, `QR`, `EAN_13`, `PDF_417`) в зависимости от задачи.

### Шаг 4: Сохранить изображение штрих‑кода в нужном формате

Aspose.BarCode позволяет выбрать формат вывода через перечисление `BarCodeImageFormat`. Ниже мы сохраняем изображение как JPEG; измените значение перечисления на `PNG`, `GIF`, `TIFF` и т.д., чтобы **конвертировать штрих‑код в GIF** или другой формат.

```java
// Save the image to your system and set its image format to JPEG.
bb.save(dataDir + "barcode-image-format.jpg", BarCodeImageFormat.JPEG);
```

**Варианты примеров**

| Желаемый формат | Значение перечисления |
|-----------------|------------------------|
| PNG             | `BarCodeImageFormat.PNG` |
| GIF             | `BarCodeImageFormat.GIF` |
| TIFF            | `BarCodeImageFormat.TIFF` |
| BMP             | `BarCodeImageFormat.BMP` |

Просто замените `BarCodeImageFormat.JPEG` на нужное значение перечисления и скорректируйте расширение файла в имени.

## Массовая генерация штрих‑кодов

Когда требуется создать сотни или тысячи этикеток, вы можете поместить описанные выше шаги в цикл и переиспользовать один экземпляр `BarcodeGenerator`. Aspose.BarCode потокобезопасен, поэтому вы также можете параллелить процесс с помощью `ExecutorService` в Java для **массовой генерации штрих‑кодов** без потери производительности.

## Распространённые сценарии использования

- **Управление запасами** — генерировать штрих‑коды продуктов «на лету» для маркировки.  
- **Системы билетов** — создавать QR или Code‑128 билеты, содержащие детали мероприятия.  
- **Обработка платежей** — внедрять коды оплаты (например, GS1 DataBar) в чеки.  
- **Автоматизация документов** — добавлять штрих‑коды в PDF, счета‑фактуры или транспортные накладные.  

## Частые проблемы и их решения

| Проблема                              | Решение |
|--------------------------------------|---------|
| *FileNotFoundException* при `save`   | Убедитесь, что `dataDir` указывает на существующую папку и приложение имеет права записи. |
| Штрих‑код выглядит размытым           | Увеличьте DPI, вызвав `bb.getParameters().setResolution(300);` перед сохранением. |
| Неправильный тип символьности         | Проверьте, что вы использовали корректное значение `EncodeTypes` для вашего формата данных. |
| Нужно прозрачное фоновое изображение | Используйте `BarCodeImageFormat.PNG` и задайте `bb.getParameters().setBackgroundColor(Color.getTransparent());` |

## Часто задаваемые вопросы

**Вопрос 1: Можно ли настроить внешний вид генерируемого штрих‑кода?**  
Ответ: Да. Aspose.BarCode предоставляет свойства для шрифта, цвета, отступов и даже добавления подписи под штрих‑кодом.

**Вопрос 2: Подходит ли Aspose.BarCode для крупномасштабных приложений?**  
Ответ: Абсолютно. Библиотека разработана для сценариев с высокой пропускной способностью и может генерировать тысячи штрих‑кодов в секунду в многопоточной среде.

**Вопрос 3: Как часто выпускаются обновления Aspose.BarCode?**  
Ответ: Библиотека регулярно получает обновления с новыми символьностями, улучшениями производительности и исправлениями ошибок. Смотрите [официальную документацию](https://reference.aspose.com/barcode/java/) для последних примечаний к выпуску.

**Вопрос 4: Можно ли попробовать Aspose.BarCode перед покупкой?**  
Ответ: Да — полностью функциональная бесплатная пробная версия доступна на [странице загрузки Aspose](https://releases.aspose.com/). Она позволяет оценить все возможности без лицензии.

**Вопрос 5: Где получить поддержку сообщества?**  
Ответ: Посетите [форум Aspose.BarCode](https://forum.aspose.com/c/barcode/13) для помощи от коллег, примеров кода и официальных ответов от команды Aspose.

## Заключение

Теперь у вас есть полное руководство **по генерации штрих‑кода**, охватывающее создание **штрих‑кода Code‑128** и сохранение его в несколько форматов изображений с помощью Aspose.BarCode for Java. Всего несколькими строками кода вы можете **конвертировать штрих‑код в GIF**, PNG, TIFF или любой другой поддерживаемый тип — делая генерацию штрих‑кода бесшовной частью ваших Java‑приложений. Экспериментируйте с другими символьностями, настраивайте параметры отрисовки и внедряйте этот фрагмент в более крупные рабочие процессы, такие как системы учёта или автоматизированные конвейеры документов.

---

**Последнее обновление:** 2026-02-04  
**Тестировано с:** Aspose.BarCode for Java 24.11  
**Автор:** Aspose  

{{< /blocks/products/pf/tutorial-page-section >}}

{{< /blocks/products/pf/main-container >}}
{{< /blocks/products/pf/main-wrap-class >}}

{{< blocks/products/products-backtop-button >}}