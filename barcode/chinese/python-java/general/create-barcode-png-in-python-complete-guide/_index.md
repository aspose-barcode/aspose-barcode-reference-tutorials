---
category: general
date: 2026-07-30
description: 使用 Python 快速创建条形码 PNG。学习如何生成 DataBar 条码、调整条码高度以及使用示例数据更改条码设置。
draft: false
images:
- PLACEHOLDER_URL/og-image.png
keywords:
- create barcode PNG
- how to change barcode
- how to generate databar
- barcode with sample data
- adjust barcode height
language: zh
lastmod: 2026-07-30
og_description: 在 Python 中创建条形码 PNG，并学习如何更改条形码高度、生成 DataBar，以及使用示例数据以获得完美结果。
og_image_alt: barcode PNG generated by Python script showing two heights
og_title: 在Python中创建条形码PNG – 逐步教程
schemas:
- author: Aspose
  dateModified: '2026-07-30'
  description: Create barcode PNG quickly with Python. Learn how to generate databar,
    adjust barcode height, and change barcode settings using sample data.
  headline: Create barcode PNG in Python – Complete Guide
  type: TechArticle
tags:
- barcode
- python
- image generation
- databar
title: 在Python中创建条形码 PNG – 完整指南
url: /zh/python-java/general/create-barcode-png-in-python-complete-guide/
---

{{< blocks/products/pf/main-wrap-class >}}
{{< blocks/products/pf/main-container >}}
{{< blocks/products/pf/tutorial-page-section >}}

# 在 Python 中创建条形码 PNG – 完整指南

是否曾经需要**创建条形码 PNG**但不确定使用哪个库或设置？你并不孤单。在许多库存或结账系统中，第一步是将原始数据转换为扫描仪喜爱的清晰 PNG 图像。  

在本教程中，我们将逐步讲解**如何生成 databar**，如何**调整条形码高度**，以及实时**更改条形码**参数的技巧——所有示例都使用**带示例数据的条形码**，你可以直接复制粘贴到项目中。  

## 您将学习

- 在 Python 中设置 Aspose.BarCode（或类似）库。  
- 用几行代码**创建条形码 PNG**文件，并可设置不同的高度。  
- **如何更改条形码**属性，如模块大小和条形码高度。  
- 使用真实的 GTIN‑14 值作为**带示例数据的条形码**。  
- 排除常见问题的技巧，如图像模糊或编码错误。

无需任何条形码生成经验——只需一个基本的 Python 解释器和安装 pip 包的能力。

## 前提条件

- 在机器上已安装 Python 3.8+。  
- 能够访问互联网以安装 `aspose-barcode` 包（或任何兼容库）。  
- 一个可写文件夹用于保存 PNG 文件（在脚本中替换 `YOUR_DIRECTORY`）。

如果你已经具备这些条件，太好了——让我们开始吧。

## 创建条形码 PNG – 设置生成器

首先，我们需要一个生成器对象，告知它我们正在处理**DataBar Omnidirectional**条形码。下面的代码导入必要的类，使用**带示例数据的条形码**创建生成器，并设置适度的模块大小。

```python
# Install the library (run once):
# pip install aspose-barcode

from aspose.barcode import BarcodeGenerator, EncodeTypes, BarCodeImageFormat

# Step 1: Create a DataBar Omnidirectional generator with sample data
# The string "(01)12345678901231" is a GTIN‑14 wrapped in Application Identifier 01.
barcode_generator = BarcodeGenerator(
    EncodeTypes.DATABAR_OMNIDIRECTIONAL,
    "(01)12345678901231"
)

# Step 2: Set common barcode parameters (module size and initial height)
# XDimension defines the width of the narrowest bar; 2 pixels is a good default.
barcode_generator.parameters.barcode.x_dimension.pixels = 2
barcode_generator.parameters.barcode.bar_height.pixels = 30   # initial 30 px height
```

**为什么这很重要：** `EncodeTypes.DATABAR_OMNIDIRECTIONAL` 告诉引擎生成一种可以从任意方向读取的**DataBar**，非常适合零售货架。设置 `x_dimension` 可保持条纹清晰，而 `bar_height` 控制代码的视觉大小。

## 如何使用示例数据生成 Databar

生成器准备就绪后，我们将**创建条形码 PNG**文件。第一次保存使用 30 像素的高度，通常足以用于屏幕预览。

```python
# Step 3: Save the barcode image at 30 px height
barcode_generator.save(r"YOUR_DIRECTORY/DatabarOmni30.png", BarCodeImageFormat.PNG)
print("30 px barcode saved as DatabarOmni30.png")
```

运行到此的脚本将生成一个看起来紧凑、高对比度的条形码 PNG 文件。使用任意图像查看器打开它，以验证条纹是否清晰且 GTIN‑14 数据已正确编码。

## 调整条形码高度（如何更改条形码）

有时需要为更大的标签或更高分辨率的打印提供更高的条形码。这时**调整条形码高度**就派上用场。更改高度只需更新 `BarHeight` 属性并再次保存即可。

```python
# Step 4: Increase the barcode height to 60 px
barcode_generator.parameters.barcode.bar_height.pixels = 60

# Step 5: Save the barcode image at the new height
barcode_generator.save(r"YOUR_DIRECTORY/DatabarOmni60.png", BarCodeImageFormat.PNG)
print("60 px barcode saved as DatabarOmni60.png")
```

**工作原理：** 生成器复用相同的数据和模块大小；仅垂直尺寸发生变化。这避免了重新创建整个对象，并演示了动态**更改条形码**设置的方法。

## 完整脚本 – 一站式解决方案

下面是完整的可运行脚本，整合了所有步骤。复制、粘贴并运行——在安装包后即可直接使用。

```python
# Full example: create two barcode PNG files with different heights

from aspose.barcode import BarcodeGenerator, EncodeTypes, BarCodeImageFormat

def create_databar_png(output_dir: str):
    # Initialize generator with sample GTIN‑14 data
    generator = BarcodeGenerator(
        EncodeTypes.DATABAR_OMNIDIRECTIONAL,
        "(01)12345678901231"
    )

    # Common settings
    generator.parameters.barcode.x_dimension.pixels = 2
    generator.parameters.barcode.bar_height.pixels = 30  # 30 px first version

    # Save first PNG (30 px)
    path30 = f"{output_dir}/DatabarOmni30.png"
    generator.save(path30, BarCodeImageFormat.PNG)
    print(f"Created barcode PNG (30 px): {path30}")

    # Change height – this is the “how to change barcode” part
    generator.parameters.barcode.bar_height.pixels = 60  # 60 px second version

    # Save second PNG (60 px)
    path60 = f"{output_dir}/DatabarOmni60.png"
    generator.save(path60, BarCodeImageFormat.PNG)
    print(f"Created barcode PNG (60 px): {path60}")

if __name__ == "__main__":
    # Replace with an actual folder you have write access to
    create_databar_png(r"YOUR_DIRECTORY")
```

### 预期输出

运行脚本将生成两个文件：

- `DatabarOmni30.png` – 高 30 px 的 DataBar Omnidirectional 条形码。  
- `DatabarOmni60.png` – 同一条形码的 60 px 高版本。

两个图像均为 PNG 格式，无损，可直接嵌入 PDF、HTML 页面，或直接打印到标签上。

## 常见陷阱与专业技巧

- **像素 vs. DPI：** PNG 文件基于像素。如果需要特定的打印尺寸，请根据 DPI 计算 `BarHeight`（例如，300 dpi → 30 px ≈ 0.1 in）。  
- **编码错误：** 确保数据字符串遵循 GS1 应用标识符格式（GTIN‑14 使用 `(01)`）。缺少括号会导致库回退为普通数字条形码。  
- **文件路径：** 在 Windows 上使用原始字符串 (`r"..."`) 或双反斜杠，以避免转义字符问题。  
- **性能：** 如果要生成成千上万的条形码，请只实例化一次生成器，仅在保存之间修改 `BarHeight` 或数据字符串。这可以减少内存消耗。  

## 下一步

现在你已经了解如何**创建条形码 PNG**，可以进一步探索：

- **如何更改条形码**颜色或添加安静区 (`generator.parameters.barcode.quiet_zone`)。  
- 导出为 SVG 或 PDF 等其他格式，以实现可伸缩打印。  
- 将脚本集成到 Flask 或 Django 接口，以按需提供条形码服务。  

这些主题都基于我们已经介绍的基础，欢迎实验并让代码随项目需求演进。

---

### 回顾

我们完整演示了在 Python 中**创建条形码 PNG**文件的全过程，展示了**如何生成 databar**，说明了简便的**调整条形码高度**方法，并解释了在不重新创建对象的情况下**更改条形码**属性。仅凭几行代码，你现在拥有一个灵活的解决方案，可用于任何条形码相关的工作流。

祝编码愉快，愿你的扫描永远无误！

## 接下来应该学习什么？

以下教程涵盖与本指南紧密相关的主题，基于所示技术进行深入。每个资源都包含完整的可运行代码示例和逐步解释，帮助你掌握更多 API 功能并在项目中探索替代实现方式。

- [如何在 Java 中创建 code128 条形码并设置条高](/barcode/english/java/barcode-configuration/setting-bars-height/)
- [如何在 Java 中生成条形码：创建精确条形码图像](/barcode/english/java/barcode-basics/creating-image-exact-barcode/)
- [创建条形码 PNG – DataMatrix 长宽比 – Aspose.BarCode](/barcode/english/net/datamatrix-barcode-configuration/datamatrix-aspect-ratio-customization/)

{{< /blocks/products/pf/tutorial-page-section >}}
{{< /blocks/products/pf/main-container >}}
{{< /blocks/products/pf/main-wrap-class >}}
{{< blocks/products/products-backtop-button >}}