---
category: general
date: 2026-09-26
description: 学习如何在 C# 中使用 DataBar Stacked Omni‑Directional 格式生成条形码图像。分步指南涵盖宽高比设置和
  PNG 导出。
draft: false
images:
- PLACEHOLDER_URL/og-image.png
keywords:
- how to generate barcode image
- C# barcode generation
- DataBar Stacked Omni‑Directional barcode
- set barcode aspect ratio
- save barcode as PNG
language: zh
lastmod: 2026-09-26
og_description: 如何使用 Aspose.BarCode 在 C# 中生成条形码图像。请按照本指南设置宽高比并导出 PNG 文件。
og_image_alt: Screenshot showing two PNG barcode images with different aspect ratios
  generated by C# code
og_title: 如何在 C# 中生成条形码图像 – 完整指南
schemas:
- author: Aspose
  dateModified: '2026-09-26'
  description: Learn how to generate barcode image in C# using the DataBar Stacked
    Omni‑Directional format. Step‑by‑step guide covers aspect‑ratio settings and PNG
    export.
  headline: How to generate barcode image in C# with DataBar Stacked Omni‑Directional
  type: TechArticle
tags:
- barcode
- C#
- Aspose.BarCode
- image export
title: 如何在 C# 中使用 DataBar Stacked Omni‑Directional 生成条码图像
url: /zh/python-java/general/how-to-generate-barcode-image-in-c-with-databar-stacked-omni/
---

{{< blocks/products/pf/main-wrap-class >}}
{{< blocks/products/pf/main-container >}}
{{< blocks/products/pf/tutorial-page-section >}}

# 如何在 C# 中使用 DataBar Stacked Omni‑Directional 生成条形码图像

如果您需要在 .NET 应用程序中 **生成条形码图像**，本教程将展示具体步骤。您将看到如何创建 DataBar Stacked Omni‑Directional 条码，调整其宽高比，并将结果导出为 PNG 文件。

生成条形码是库存系统、零售 POS 终端和物流跟踪的常见需求。在接下来的章节中，我们将覆盖您需要的全部内容——从所需的 NuGet 包到最终的图像验证——让您能够在任何 C# 项目中集成条形码生成。

## 前置条件

在开始之前，请确保您拥有：

* .NET 6.0 或更高（代码兼容 .NET Core 和 .NET Framework）
* Visual Studio 2022 或您喜欢的任何 C# IDE
* **Aspose.BarCode for .NET** NuGet 包（使用 `dotnet add package Aspose.BarCode` 安装）
* 对保存 PNG 文件的文件夹拥有写入权限

这些前置条件确保示例可以在无需额外配置的情况下运行。

## 步骤 1：设置输出文件夹

您首先需要一个目录来写入生成的 PNG 文件。使用绝对路径或相对路径均可，但请确保在调用 `Save` 之前文件夹已存在。

```csharp
// Step 1: Define the output folder
string outputFolder = Path.Combine(Environment.CurrentDirectory, "Barcodes");

// Create the folder if it doesn't exist
Directory.CreateDirectory(outputFolder);
```

以编程方式创建文件夹可防止在 `Save` 方法尝试写入图像时出现 *FileNotFoundException*。

## 步骤 2：创建 DataBar Stacked Omni‑Directional 条码生成器

**BarcodeGenerator** 类是所有条码类型的入口。在这里我们指定 `EncodeTypes.DatabarStackedOmniDirectional` 并提供要编码的数据字符串。

```csharp
// Step 2: Initialise the generator with the desired barcode type and data
BarcodeGenerator generator = new BarcodeGenerator(
    EncodeTypes.DatabarStackedOmniDirectional,
    "(01)12345678901231"); // GS1 Application Identifier 01 + 14‑digit GTIN
```

*为什么重要*：`EncodeTypes` 枚举告诉库要渲染哪种符号。使用 Omni‑Directional 变体可确保条码能够从任何方向被扫描——这是一项许多仓库扫描器的需求。

## 步骤 3：配置通用条码参数

可读的条码需要合理的 X‑dimension（最小条的宽度）。将 `Pixels` 设置为 2 可在大多数屏幕分辨率下在尺寸和清晰度之间取得良好平衡。

```csharp
// Step 3: Set common barcode parameters (pixel size of the X‑dimension)
generator.Parameters.Barcode.XDimension.Pixels = 2;
```

如果需要更大或更小的图像，您可以随后调整 `Pixels`；库会自动缩放条码的其余部分。

## 步骤 4：使用宽高比 15 生成第一张图像

**宽高比** 控制 DataBar 条码的高宽比例。宽高比为 15 是零售标签的常见默认值。

```csharp
// Step 4: Configure aspect ratio 15 and save the image
generator.Parameters.Barcode.DataBar.AspectRatio = 15;
generator.Save($"{outputFolder}/DatabarAspectRatio15.png", BarCodeImageFormat.Png);
```

调用此方法后，您将在输出文件夹中看到 `DatabarAspectRatio15.png`。该图像为无损 PNG，适合进一步处理或打印。

## 步骤 5：将宽高比改为 30 并导出第二张图像

有时需要更高的条码——例如标签高度受限时。将宽高比改为 30 可在保持数据完整性的同时产生更紧凑的视觉效果。

```csharp
// Step 5: Change aspect ratio to 30 and save the second image
generator.Parameters.Barcode.DataBar.AspectRatio = 30;
generator.Save($"{outputFolder}/DatabarAspectRatio30.png", BarCodeImageFormat.Png);
```

现在您拥有两张 PNG 文件，展示相同数据在不同宽高比下的效果。并排比较可帮助您决定哪种变体最适合您的标签布局。

## 完整、可运行的示例

下面是完整的程序示例，您可以复制、粘贴并运行。它包含所有 `using` 指令、错误处理以及解释每一步的注释。

```csharp
using System;
using System.IO;
using Aspose.BarCode;
using Aspose.BarCode.Generation;

class Program
{
    static void Main()
    {
        try
        {
            // 1️⃣ Define the output folder
            string outputFolder = Path.Combine(Environment.CurrentDirectory, "Barcodes");
            Directory.CreateDirectory(outputFolder);

            // 2️⃣ Initialise the barcode generator (DataBar Stacked Omni‑Directional)
            BarcodeGenerator generator = new BarcodeGenerator(
                EncodeTypes.DatabarStackedOmniDirectional,
                "(01)12345678901231"); // GS1 GTIN

            // 3️⃣ Set common parameters – X‑dimension in pixels
            generator.Parameters.Barcode.XDimension.Pixels = 2;

            // 4️⃣ First image: aspect ratio 15
            generator.Parameters.Barcode.DataBar.AspectRatio = 15;
            string file15 = Path.Combine(outputFolder, "DatabarAspectRatio15.png");
            generator.Save(file15, BarCodeImageFormat.Png);
            Console.WriteLine($"Saved barcode with aspect ratio 15 to: {file15}");

            // 5️⃣ Second image: aspect ratio 30
            generator.Parameters.Barcode.DataBar.AspectRatio = 30;
            string file30 = Path.Combine(outputFolder, "DatabarAspectRatio30.png");
            generator.Save(file30, BarCodeImageFormat.Png);
            Console.WriteLine($"Saved barcode with aspect ratio 30 to: {file30}");
        }
        catch (Exception ex)
        {
            Console.Error.WriteLine($"Error generating barcode: {ex.Message}");
        }
    }
}
```

**预期输出**（控制台）：

```
Saved barcode with aspect ratio 15 to: C:\YourPath\Barcodes\DatabarAspectRatio15.png
Saved barcode with aspect ratio 30 to: C:\YourPath\Barcodes\DatabarAspectRatio30.png
```

在任意图像查看器中打开这两张 PNG 文件。您会看到相同的 GTIN 已被编码，但第二张图像因更高的宽高比而更高。

## 专业提示与常见陷阱

| 情况 | 建议 |
|-----------|----------------|
| **低分辨率显示器** | 将 `XDimension.Pixels` 提升至 3 或 4，以保持条纹清晰。 |
| **在热敏纸上打印** | 使用 `BarCodeImageFormat.Tiff` 获得更高保真度，并将 `Resolution.DpiX/DpiY` 设置为 300。 |
| **动态数据** | 重用同一个 `BarcodeGenerator` 实例，并在每次 `Save` 前仅更新 `generator.CodeText`。这可避免不必要的对象分配。 |
| **无效的 GTIN** | 若数据不符合 GS1 标准，库会抛出 `ArgumentException`。请在传递给生成器前验证数据。 |

## 结论

您现在已经了解如何使用 Aspose.BarCode 在 C# 中 **生成条形码图像**，如何控制 DataBar Stacked Omni‑Directional 的宽高比，以及如何将结果导出为 PNG 文件。此端到端示例涵盖了安装、配置和错误处理，为任何条码相关功能提供了坚实的基础。

接下来，您可以探索：

* **C# 条码生成**，用于其他 **符号** 如 QR、Code 128 或 PDF417。
* 使用 Aspose.PDF 将条码 **嵌入 PDF** 文档。
* **批量处理** 多个数据字符串并将其保存为单个 **多页 PDF**。

欢迎尝试不同的 `EncodeTypes`、图像格式和分辨率设置，以匹配项目的具体需求。祝编码愉快！

## 接下来您应该学习什么？

以下教程涵盖与本指南紧密相关的主题，基于所示技术进行扩展。每个资源都包含完整的可运行代码示例和逐步说明，帮助您掌握更多 API 功能并在项目中探索替代实现方案。

- [databar 扩展堆叠条码指南 – 如何在 C# 中生成和设置大小](/barcode/english/python-java/general/databar-expanded-stacked-barcode-guide-how-to-generate-and-s/)
- [如何使用 Aspose.BarCode for .NET 生成并调整一维 Databar 条码高度](/barcode/english/net/one-dimensional-barcode-types/one-dimensional-databar-barcode-height-adjustment/)
- [如何在 C# 中使用 Aspose.BarCode 生成条形码图像](/barcode/english/python-java/general/how-to-generate-barcode-image-with-aspose-barcode-in-c/)

{{< /blocks/products/pf/tutorial-page-section >}}
{{< /blocks/products/pf/main-container >}}
{{< /blocks/products/pf/main-wrap-class >}}
{{< blocks/products/products-backtop-button >}}