---
category: general
date: 2026-09-26
description: 學習如何在 C# 中使用 DataBar 堆疊全方向格式產生條碼圖像。逐步指南涵蓋長寬比設定與 PNG 匯出。
draft: false
images:
- PLACEHOLDER_URL/og-image.png
keywords:
- how to generate barcode image
- C# barcode generation
- DataBar Stacked Omni‑Directional barcode
- set barcode aspect ratio
- save barcode as PNG
language: zh-hant
lastmod: 2026-09-26
og_description: 如何在 C# 中使用 Aspose.BarCode 產生條碼圖像。請遵循本指南設定長寬比並匯出 PNG 檔案。
og_image_alt: Screenshot showing two PNG barcode images with different aspect ratios
  generated by C# code
og_title: 如何在 C# 中生成條碼圖像 – 完整指南
schemas:
- author: Aspose
  dateModified: '2026-09-26'
  description: Learn how to generate barcode image in C# using the DataBar Stacked
    Omni‑Directional format. Step‑by‑step guide covers aspect‑ratio settings and PNG
    export.
  headline: How to generate barcode image in C# with DataBar Stacked Omni‑Directional
  type: TechArticle
tags:
- barcode
- C#
- Aspose.BarCode
- image export
title: 如何在 C# 中使用 DataBar Stacked Omni‑Directional 產生條碼圖像
url: /zh-hant/python-java/general/how-to-generate-barcode-image-in-c-with-databar-stacked-omni/
---

{{< blocks/products/pf/main-wrap-class >}}
{{< blocks/products/pf/main-container >}}
{{< blocks/products/pf/tutorial-page-section >}}

# 如何在 C# 中使用 DataBar Stacked Omni‑Directional 產生條碼圖像

如果您需要在 .NET 應用程式中 **如何產生條碼圖像**，本教學將展示完整步驟。您將看到如何建立 DataBar Stacked Omni‑Directional 條碼、調整其長寬比，並將結果匯出為 PNG 檔案。

產生條碼是庫存系統、零售 POS 終端機與物流追蹤的常見需求。以下各節將從所需的 NuGet 套件說明到最終圖像驗證，完整覆蓋您在任何 C# 專案中整合條碼產生的所有步驟。

## 前置條件

在開始之前，請確保您已具備：

* .NET 6.0 或更新版本（程式碼同樣適用於 .NET Core 與 .NET Framework）
* Visual Studio 2022 或您慣用的 C# IDE
* **Aspose.BarCode for .NET** NuGet 套件（使用 `dotnet add package Aspose.BarCode` 安裝）
* 對將要儲存 PNG 檔案的資料夾具有寫入權限

上述前置條件可確保範例在不需額外設定的情況下順利執行。

## 第一步：設定輸出資料夾

首先需要一個目錄來寫入產生的 PNG 檔案。使用絕對路徑或相對路徑皆可，但在呼叫 `Save` 前務必確保資料夾已存在。

```csharp
// Step 1: Define the output folder
string outputFolder = Path.Combine(Environment.CurrentDirectory, "Barcodes");

// Create the folder if it doesn't exist
Directory.CreateDirectory(outputFolder);
```

以程式方式建立資料夾可避免在 `Save` 方法寫入圖像時拋出 *FileNotFoundException*。

## 第二步：建立 DataBar Stacked Omni‑Directional 條碼產生器

**BarcodeGenerator** 類別是所有條碼類型的入口點。此處我們指定 `EncodeTypes.DatabarStackedOmniDirectional` 以及欲編碼的資料字串。

```csharp
// Step 2: Initialise the generator with the desired barcode type and data
BarcodeGenerator generator = new BarcodeGenerator(
    EncodeTypes.DatabarStackedOmniDirectional,
    "(01)12345678901231"); // GS1 Application Identifier 01 + 14‑digit GTIN
```

*為什麼重要*：`EncodeTypes` 列舉告訴函式庫要渲染哪種符號。使用 Omni‑Directional 變體可確保條碼在任何方向皆可被掃描，這是許多倉儲掃描器的必備條件。

## 第三步：設定通用條碼參數

可讀的條碼需要合理的 X‑dimension（最小條寬）。將 `Pixels` 設為 2 可在大多數螢幕解析度下取得尺寸與清晰度的良好平衡。

```csharp
// Step 3: Set common barcode parameters (pixel size of the X‑dimension)
generator.Parameters.Barcode.XDimension.Pixels = 2;
```

若需要更大或更小的圖像，可稍後調整 `Pixels`；函式庫會自動縮放條碼的其餘部分。

## 第四步：產生長寬比為 15 的第一張圖像

**aspect ratio** 控制 DataBar 條碼的高寬比例。長寬比 15 是零售標籤的常見預設值。

```csharp
// Step 4: Configure aspect ratio 15 and save the image
generator.Parameters.Barcode.DataBar.AspectRatio = 15;
generator.Save($"{outputFolder}/DatabarAspectRatio15.png", BarCodeImageFormat.Png);
```

執行此呼叫後，您會在輸出資料夾中看到 `DatabarAspectRatio15.png`。該圖像為無損 PNG，適合後續處理或列印。

## 第五步：將長寬比改為 30 並匯出第二張圖像

有時需要較高的條碼，例如標籤高度受限時。將長寬比調整為 30 可在保持資料完整性的同時產生更緊湊的視覺效果。

```csharp
// Step 5: Change aspect ratio to 30 and save the second image
generator.Parameters.Barcode.DataBar.AspectRatio = 30;
generator.Save($"{outputFolder}/DatabarAspectRatio30.png", BarCodeImageFormat.Png);
```

現在您擁有兩個 PNG 檔，分別展示相同資料在不同長寬比下的外觀。此並排比較有助於決定哪個變體最適合您的標籤版面。

## 完整可執行範例

以下程式碼為完整範例，您可直接複製、貼上並執行。內含所有 `using` 指令、錯誤處理與說明每個步驟的註解。

```csharp
using System;
using System.IO;
using Aspose.BarCode;
using Aspose.BarCode.Generation;

class Program
{
    static void Main()
    {
        try
        {
            // 1️⃣ Define the output folder
            string outputFolder = Path.Combine(Environment.CurrentDirectory, "Barcodes");
            Directory.CreateDirectory(outputFolder);

            // 2️⃣ Initialise the barcode generator (DataBar Stacked Omni‑Directional)
            BarcodeGenerator generator = new BarcodeGenerator(
                EncodeTypes.DatabarStackedOmniDirectional,
                "(01)12345678901231"); // GS1 GTIN

            // 3️⃣ Set common parameters – X‑dimension in pixels
            generator.Parameters.Barcode.XDimension.Pixels = 2;

            // 4️⃣ First image: aspect ratio 15
            generator.Parameters.Barcode.DataBar.AspectRatio = 15;
            string file15 = Path.Combine(outputFolder, "DatabarAspectRatio15.png");
            generator.Save(file15, BarCodeImageFormat.Png);
            Console.WriteLine($"Saved barcode with aspect ratio 15 to: {file15}");

            // 5️⃣ Second image: aspect ratio 30
            generator.Parameters.Barcode.DataBar.AspectRatio = 30;
            string file30 = Path.Combine(outputFolder, "DatabarAspectRatio30.png");
            generator.Save(file30, BarCodeImageFormat.Png);
            Console.WriteLine($"Saved barcode with aspect ratio 30 to: {file30}");
        }
        catch (Exception ex)
        {
            Console.Error.WriteLine($"Error generating barcode: {ex.Message}");
        }
    }
}
```

**預期輸出**（主控台）：

```
Saved barcode with aspect ratio 15 to: C:\YourPath\Barcodes\DatabarAspectRatio15.png
Saved barcode with aspect ratio 30 to: C:\YourPath\Barcodes\DatabarAspectRatio30.png
```

使用任意圖像檢視器開啟兩個 PNG 檔。您會看到相同的 GTIN 已被編碼，但第二張圖因長寬比較高而顯得較長。

## 專業提示與常見陷阱

| 情境 | 建議 |
|-----------|----------------|
| **低解析度螢幕** | 將 `XDimension.Pixels` 提升至 3 或 4，以保持條紋清晰。 |
| **熱感紙列印** | 使用 `BarCodeImageFormat.Tiff` 取得更高保真度，並將 `Resolution.DpiX/DpiY` 設為 300。 |
| **動態資料** | 重複使用同一個 `BarcodeGenerator` 實例，僅在每次 `Save` 前更新 `generator.CodeText`，可避免不必要的物件分配。 |
| **GTIN 無效** | 若資料不符合 GS1 標準，函式庫會拋出 `ArgumentException`。請在傳入產生器前先驗證資料。 |

## 結論

您現在已掌握 **如何在 C# 中使用 Aspose.BarCode 產生條碼圖像**、如何控制 DataBar Stacked Omni‑Directional 的長寬比，以及如何將結果匯出為 PNG 檔案。此端對端範例涵蓋安裝、設定與錯誤處理，為任何條碼相關功能奠定堅實基礎。

接下來，您可以探索：

* **C# 條碼產生**：其他符號如 QR、Code 128 或 PDF417。
* **在 PDF 文件中嵌入條碼**：使用 Aspose.PDF。
* **批次處理**：一次處理多筆資料並儲存為單一多頁 PDF。

歡迎嘗試不同的 `EncodeTypes`、圖像格式與解析度設定，以符合您專案的精確需求。祝開發順利！

## 接下來您應該學習什麼？

以下教學與本指南緊密相關，能進一步深化您對 API 功能的掌握，並提供替代實作方式的完整範例與步驟說明。

- [databar expanded stacked 條碼指南 – 如何在 C# 中產生與調整大小](/barcode/english/python-java/general/databar-expanded-stacked-barcode-guide-how-to-generate-and-s/)
- [如何使用 Aspose.BarCode for .NET 產生與調整一維 Databar 條碼高度](/barcode/english/net/one-dimensional-barcode-types/one-dimensional-databar-barcode-height-adjustment/)
- [如何在 C# 中使用 Aspose.BarCode 產生條碼圖像](/barcode/english/python-java/general/how-to-generate-barcode-image-with-aspose-barcode-in-c/)

{{< /blocks/products/pf/tutorial-page-section >}}
{{< /blocks/products/pf/main-container >}}
{{< /blocks/products/pf/main-wrap-class >}}
{{< blocks/products/products-backtop-button >}}