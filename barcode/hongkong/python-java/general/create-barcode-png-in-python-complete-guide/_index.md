---
category: general
date: 2026-07-30
description: 使用 Python 快速生成條碼 PNG。學習如何產生 DataBar、調整條碼高度，並使用範例資料更改條碼設定。
draft: false
images:
- PLACEHOLDER_URL/og-image.png
keywords:
- create barcode PNG
- how to change barcode
- how to generate databar
- barcode with sample data
- adjust barcode height
language: zh-hant
lastmod: 2026-07-30
og_description: 在 Python 中建立條碼 PNG，並學習如何調整條碼高度、生成 DataBar，以及使用範例資料以獲得完美結果。
og_image_alt: barcode PNG generated by Python script showing two heights
og_title: 在 Python 中建立條碼 PNG – 步驟教學
schemas:
- author: Aspose
  dateModified: '2026-07-30'
  description: Create barcode PNG quickly with Python. Learn how to generate databar,
    adjust barcode height, and change barcode settings using sample data.
  headline: Create barcode PNG in Python – Complete Guide
  type: TechArticle
tags:
- barcode
- python
- image generation
- databar
title: 在 Python 中建立條碼 PNG – 完整指南
url: /zh-hant/python-java/general/create-barcode-png-in-python-complete-guide/
---

{{< blocks/products/pf/main-wrap-class >}}
{{< blocks/products/pf/main-container >}}
{{< blocks/products/pf/tutorial-page-section >}}

# 在 Python 中建立條碼 PNG – 完整指南

曾經需要 **create barcode PNG**，卻不確定該使用哪個函式庫或設定嗎？你並不孤單。在許多庫存或結帳系統中，第一步就是將原始資料轉換成掃描器喜愛的清晰 PNG 圖片。

在本教學中，我們將一步步說明 **how to generate databar**、**adjust barcode height**，以及即時 **change barcode** 參數的技巧——全部使用可直接複製貼上的 **barcode with sample data** 範例。

## 你將學會什麼

- 在 Python 中設定 Aspose.BarCode（或類似）函式庫。  
- 用幾行程式碼 **Create barcode PNG**，並調整不同高度。  
- **How to change barcode** 的屬性，如模組大小與條碼高度。  
- 使用真實的 GTIN‑14 值作為 **barcode with sample data**。  
- 解決模糊影像或編碼錯誤等常見問題的技巧。

不需要任何條碼產生的先前經驗——只要有基本的 Python 直譯器與安裝 pip 套件的能力即可。

## 前置條件

- 已在電腦上安裝 Python 3.8 以上版本。  
- 能連上網路以下載 `aspose-barcode` 套件（或任何相容函式庫）。  
- 有可寫入的資料夾供 PNG 檔案儲存（請在腳本中將 `YOUR_DIRECTORY` 替換成實際路徑）。

如果上述條件皆已具備，太好了——讓我們開始吧。

## Create barcode PNG – 設定產生器

首先，我們需要一個產生器物件，讓它知道我們要產生 **DataBar Omnidirectional** 條碼。以下程式碼會匯入必要的類別，使用 **barcode with sample data** 建立產生器，並設定適中的模組大小。

```python
# Install the library (run once):
# pip install aspose-barcode

from aspose.barcode import BarcodeGenerator, EncodeTypes, BarCodeImageFormat

# Step 1: Create a DataBar Omnidirectional generator with sample data
# The string "(01)12345678901231" is a GTIN‑14 wrapped in Application Identifier 01.
barcode_generator = BarcodeGenerator(
    EncodeTypes.DATABAR_OMNIDIRECTIONAL,
    "(01)12345678901231"
)

# Step 2: Set common barcode parameters (module size and initial height)
# XDimension defines the width of the narrowest bar; 2 pixels is a good default.
barcode_generator.parameters.barcode.x_dimension.pixels = 2
barcode_generator.parameters.barcode.bar_height.pixels = 30   # initial 30 px height
```

**為什麼這很重要：** `EncodeTypes.DATABAR_OMNIDIRECTIONAL` 會告訴引擎產生可從任意方向讀取的 **DataBar**，非常適合零售貨架。設定 `x_dimension` 可保持條紋銳利，而 `bar_height` 則控制條碼的視覺大小。

## 如何使用範例資料產生 Databar

產生器準備好後，我們就可以 **create barcode PNG**。第一個儲存使用 30 像素的高度，通常足以作為螢幕預覽。

```python
# Step 3: Save the barcode image at 30 px height
barcode_generator.save(r"YOUR_DIRECTORY/DatabarOmni30.png", BarCodeImageFormat.PNG)
print("30 px barcode saved as DatabarOmni30.png")
```

執行到此的腳本會產生一個外觀緊湊、對比度高的 PNG 檔案。用任何圖像檢視器開啟，確認條紋清晰且 GTIN‑14 資料已正確編碼。

## 調整條碼高度（How to Change Barcode）

有時需要較高的條碼以配合較大的標籤或高解析度列印。這時 **adjust barcode height** 就派上用場。只要更新 `BarHeight` 屬性再儲存一次即可。

```python
# Step 4: Increase the barcode height to 60 px
barcode_generator.parameters.barcode.bar_height.pixels = 60

# Step 5: Save the barcode image at the new height
barcode_generator.save(r"YOUR_DIRECTORY/DatabarOmni60.png", BarCodeImageFormat.PNG)
print("60 px barcode saved as DatabarOmni60.png")
```

**運作原理：** 產生器會重複使用相同的資料與模組大小，僅改變垂直尺寸。這樣不必重新建立整個物件，亦示範了 **how to change barcode** 設定的動態調整方式。

## 完整腳本 – 一站式解決方案

以下是結合所有步驟的完整可執行腳本。直接複製、貼上並執行——安裝套件後即可立即運作。

```python
# Full example: create two barcode PNG files with different heights

from aspose.barcode import BarcodeGenerator, EncodeTypes, BarCodeImageFormat

def create_databar_png(output_dir: str):
    # Initialize generator with sample GTIN‑14 data
    generator = BarcodeGenerator(
        EncodeTypes.DATABAR_OMNIDIRECTIONAL,
        "(01)12345678901231"
    )

    # Common settings
    generator.parameters.barcode.x_dimension.pixels = 2
    generator.parameters.barcode.bar_height.pixels = 30  # 30 px first version

    # Save first PNG (30 px)
    path30 = f"{output_dir}/DatabarOmni30.png"
    generator.save(path30, BarCodeImageFormat.PNG)
    print(f"Created barcode PNG (30 px): {path30}")

    # Change height – this is the “how to change barcode” part
    generator.parameters.barcode.bar_height.pixels = 60  # 60 px second version

    # Save second PNG (60 px)
    path60 = f"{output_dir}/DatabarOmni60.png"
    generator.save(path60, BarCodeImageFormat.PNG)
    print(f"Created barcode PNG (60 px): {path60}")

if __name__ == "__main__":
    # Replace with an actual folder you have write access to
    create_databar_png(r"YOUR_DIRECTORY")
```

### 預期輸出

執行腳本後會產生兩個檔案：

- `DatabarOmni30.png` – 高度 30 px 的 DataBar Omnidirectional 條碼。  
- `DatabarOmni60.png` – 同一條碼的 60 px 高版本。

兩個影像皆為 PNG 格式、無損壓縮，可直接嵌入 PDF、HTML 頁面，或直接列印到標籤上。

## 常見問題與專業提示

- **Pixel vs. DPI：** PNG 為像素為單位。若需特定列印尺寸，請依 DPI 計算 `BarHeight`（例如 300 dpi → 30 px ≈ 0.1 in）。  
- **編碼錯誤：** 確認資料字串符合 GS1 應用識別碼格式（`(01)` 為 GTIN‑14）。缺少括號會導致函式庫退回純數字條碼。  
- **檔案路徑：** 在 Windows 上使用原始字串 (`r"..."`) 或雙反斜線，以避免跳脫字元問題。  
- **效能：** 若要產生上千條條碼，建議只實例化一次產生器，然後在每次儲存前僅修改 `BarHeight` 或資料字串，減少記憶體開銷。

## 下一步

既然已掌握 **create barcode PNG**，你可以進一步探索：

- **How to change barcode** 的顏色或加入靜區 (`generator.parameters.barcode.quiet_zone`)。  
- 匯出為 SVG 或 PDF 等可縮放格式，以利列印。  
- 將腳本整合至 Flask 或 Django 端點，提供即時條碼服務。

上述主題皆建立在本篇所述的基礎上，歡迎自行實驗，讓程式碼隨專案需求不斷演進。

---

### 小結

我們完整示範了在 Python 中 **create barcode PNG** 的全流程，說明了 **how to generate databar**、簡易的 **adjust barcode height** 方法，以及 **how to change barcode** 屬性而不必重新建立物件。只需幾行程式碼，你就擁有彈性的條碼解決方案，適用於任何條碼相關工作流程。

祝編程順利，掃描永遠無誤！

## 接下來該學什麼？

以下教學與本指南緊密相關，並提供完整可執行的程式碼範例與逐步說明，協助你掌握更多 API 功能，或在專案中探索其他實作方式。

- [How to create code128 barcode Java and set bar height](/barcode/english/java/barcode-configuration/setting-bars-height/)
- [How to generate barcode java: Create an Exact Barcode Image](/barcode/english/java/barcode-basics/creating-image-exact-barcode/)
- [Create Barcode PNG – DataMatrix Aspect Ratio – Aspose.BarCode](/barcode/english/net/datamatrix-barcode-configuration/datamatrix-aspect-ratio-customization/)

{{< /blocks/products/pf/tutorial-page-section >}}
{{< /blocks/products/pf/main-container >}}
{{< /blocks/products/pf/main-wrap-class >}}
{{< blocks/products/products-backtop-button >}}